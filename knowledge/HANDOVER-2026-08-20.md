# Customer 360 — Handover 2026-08-20 (Dreamforce countdown)

Supersedes nothing; extends HANDOVER-2026-08-14. Read order: STATUS.md → this file →
DREAMFORCE-SPIN-CONTEXT.md → HANDOFF-2026-07-27.md for July build depth.

## 1. Urgency

**C360 is Demo 2 (COMMERCIAL banking) at the SPIN Dreamforce booth, Sept 15-16, 2026 — ~4 weeks out.**
The print-approved journey panel already promises our exact moment in ink: *"a client email
'increase our line from 15 to 20M' arrives via the M365 connector and becomes a governed action a
banker approves — shown live."* The demo MUST deliver that beat. No voice in this demo (voice is
Demo 1's). Full booth context: DREAMFORCE-SPIN-CONTEXT.md.

## 2. Access runbook (everything a fresh session needs)

| What | How |
|---|---|
| **Repo (single source)** | `cowork-offering/customer-360` (private). Archy box: push as fabiangoetzens via `GH_CONFIG_DIR=/home/fabian/.config/gh-claudeeshannon`. Banksy: stored x-access-token credential works for pull/push. |
| **Banksy (dev seat)** | `ssh ubuntu@98.87.86.133` (keys: archy box, brain-founders, fabian@macbook, noland@macbook-m5). Clone at `/home/ubuntu/projects/customer-360`. RULES: everything ubuntu-owned, NEVER sudo for git/cp; brain writes via `/opt/brain/scripts/brain-write.sh`; ontology via markers in `state/ontology-pending/` + `ontology-drain.sh` (deterministic) or `brain-ontology.py` (types: see LINK_TYPES in script); index refresh = `lance-nightly-rebuild.sh` as ubuntu (nightly 04:00, safe to run ad-hoc; daemon needs ~1-2 min warm-up after); verify with `memory-recall.sh` probes — silence is not success. |
| **Banksy brain docs** | `/opt/brain/knowledge/projects/customer-360/` — keep in sync with repo `knowledge/` after every push (cp from the clone). |
| **Archy box (org seat)** | `/opt/connectry/projects/commercial-credit-reinvented/customer-360-reinvented` (same repo). **sf CLI auth to `bankinggpt` lives HERE ONLY** (`-o bankinggpt`). All org work runs from Archy or gets deliberate new auth — never improvised. |
| **Org** | `bankinggpt` — SHARED Accenture sandbox. Never touch pre-existing build; additive deploys with receipts; probes on throwaway data, residue zero. Rebuild mirror: `knowledge/sf-build-v2/wp2/`. |
| **Artifact URLs** | main `claude.ai/code/artifact/f7a6006f-214c-44b5-bf66-9d75fba6e02e`, copy `95cf2a8d-1b27-4213-b516-35b84cb70651`. Republishable only from the owning conversation or with `url:`; 409 guard on conflicts — never force without founder say-so. |
| **Plugin** | Served from cowork-offering marketplace (`cowork-plugins`, entry `client-360` v0.4.2, git-subdir repo root). Install: add marketplace → install Credit 360. |
| **Publish pipeline** | `app: npm run build` → `node scripts/release-artifact.mjs` → `node app/scripts/assemble-artifact.mjs` (ONLY this — full-tag marker, slot-verified) → Artifact republish both URLs. QA gate: typecheck, vitest (~1,200), contrast, clean-room `npm ci`. |

## 3. Situation brief — verified vs theoretical (the honest ledger)

**What WAS live-proven in July, with receipts (not theoretical):**
- All 9 reads against the real org; Hartwell + Piedmont bundles baked from live-observed envelopes.
- Real writes executed and verified in nCino: collateral valuations **CV-0000000002** (first
  banker-originated write, by Fabian, 2026-07-26) and CV-0000000003; risk-rating review **R-4**.
- **New facility end-to-end live**: package-first + borrowing structure, two-invocation resume,
  real records created then tree-deleted (probe discipline).
- Bulk valuation staged live (items[], one token) — envelope archived; facilityIds[] package plans
  staged live incl. a live-captured refusal (`sf-build-v2/wp2/observed-envelopes-facilityIds.json`).
- Apex suite 170/170 at last run (Jul 28); cockpit ~1,200 tests at 20bf17a.

**What has NEVER been executed (genuinely theoretical):**
- **Loan modification EXECUTE and renewal EXECUTE** — stage-only by design (LV06/Booked wall).
  A modification has never been *filed* through the cockpit, only staged/planned.
- **Covenant review execute** — deployed but out of manifest; first live run fires a real approval
  email at a real human (founder-gated).
- **The demo golden path end-to-end by a human**: email arrives → sync pulls it → suggestion →
  prefilled package-anchored modification ticket → staged plan → confirm. Fabian's 100%-certain
  click-through never happened. THE PRINTED DEMO BEAT HAS NEVER BEEN REHEARSED LIVE.

**What is STALE (was proven, but nobody has looked since Jul 29 — shared-sandbox drift risk):**
- Org state: tool deployment, Hartwell's 91 records (18/18 counts), McpServerDefinition, permset.
- The artifact URLs' live connector calls (last exercised early Aug).
- Apex suite: not re-run in 3+ weeks.

## 4. VERIFICATION CAMPAIGN — the next session's first job (ordered, safety-fenced)

Run from Archy (org auth). Evidence for every step; no done-claims without receipts.

0. **Re-verify July's own receipts** — the claimed artifacts must still exist: CV-0000000002,
   CV-0000000003 (collateral valuations), R-4 (risk-rating review), the Customer360
   McpServerDefinition, the C360_Action_Staging_Access permset. July's proof is only proof if it
   is still standing.
1. **Org intact?** `Customer360` McpServerDefinition present; count Customer360*/C360*/Stage*/
   Execute* classes (expect 24 tools + engine); Hartwell verification counts vs
   DEMO-RELATIONSHIP.md (18 checks); permset assignment.
2. **Apex suite re-run** (expect 170/170; any drift = shared-sandbox interference, diagnose
   before anything else).
3. **Reads live**: Actions-API smoke on Portfolio/Snapshot/Exposure/Covenants for Hartwell —
   figures still match the baked bundle ($46.0MM, 6 booked, 4+2 covenants).
4. **Stage a REAL modification on a Hartwell booked loan** (facilityIds[], the $15M LoC) —
   stage = zero domain DML, safe; verify plan/token/hash shape; delete the staging row.
   This converts the demo's core write path from theoretical to observed. DO NOT execute.
5. **Bulk valuation stage** on 2 Hartwell collateral items — same discipline.
6. **Artifact live check** (needs Fabian's browser): open main URL, Sync Hartwell, confirm
   connector calls succeed and the M365 email lands as CLIENT REQUEST → suggestion → prefilled
   ticket. Stopwatch it — this is the demo beat.
7. **Report**: table of step / expected / observed / receipt (record ids, run ids, timings).
   Then and only then: rehearsal planning.

**Safety fences:** no execute of mod/renewal/covenant (gated); no compliance-row creation (fires
unrecallable email at robert.mcclaren@outlook.com); Hartwell is permanent — create-then-delete only
on staging rows; ⚠️ renewal clone re-probe (Loan_Collateral_Aggregate) required before
execute_renewal is EVER considered.

## 5. Agreed outstanding list (as of 2026-08-20, from STATUS)

1. Plugin skill-prose sync → v0.5.0 (bundle already fresh via marketplace; prose says 8 tools, 24 exist).
2. Fabian click-through (folds into campaign step 6).
3. Codex adversarial review (never run — schedule before Dreamforce freeze).
4. ⚠️ Renewal clone re-probe before execute_renewal.
5. Covenant execute first live run (founder-gated).
6. Mod/renewal execute unblock decision (design question, post-verification).
7. Housekeeping: Piedmont test rows; old public `weareconnectry/customer360-cockpit` stale repo
   (archive-or-sync decision open); Credit Memo 0.54.0 swap (sibling).
8. SR 11-7 → **SR 26-2** citation currency (diff control set first).
9. Demo script: STILL on Fabian's hold — journey map exists (see chat/Design assets), script does not.

## 6. Kickoff prompt for the next session (paste as-is)

We are 4 weeks from Dreamforce; Customer 360 is Demo 2 (commercial banking) at the SPIN booth and
the printed journey panel promises our email-to-action moment live. Ground yourself: read STATUS.md,
knowledge/HANDOVER-2026-08-20.md (this file), knowledge/DREAMFORCE-SPIN-CONTEXT.md. Then execute
the VERIFICATION CAMPAIGN in HANDOVER-2026-08-20.md §4 exactly as ordered, from the Archy box,
with evidence per step and the safety fences respected. Do not execute gated writes; do not touch
Hartwell records except staging rows you delete; report the verification table before proposing
any build work. After the campaign: plugin skill-prose sync to v0.5.0, then Codex review.
