# Customer 360 — Handover, 2026-08-14 (Banksy era begins)

Delta on top of `HANDOFF-2026-07-27.md` (which stays authoritative for the July build detail).
Read order for a fresh session: `STATUS.md` (root) → this file → HANDOFF-2026-07-27.md as needed.

## What changed since the July handoff

1. **July 28-29 waves shipped** (by the Archy-side sessions): Exposure v2 coverage-correctness
   (pledged-share math, honest nulls), `facilityIds[]` package plans deployed + wired to observed
   envelopes, KYC & Onboarding zone, unified action surface, ~1,200 cockpit tests, Apex 170/170.
   The published artifact URLs were verified **byte-identical** to repo HEAD `20bf17a` on 2026-08-14
   (data slot AND compiled bundle SHA-matched).
2. **Delivery model clarified** (a confusion worth never repeating): three surfaces, one bundle.
   (a) claude.ai artifact URLs — runtime connector calls via `window.claude.mcp`, republished by the
   owning session; (b) the **Cowork plugin** (`.claude-plugin/` in THIS repo) — bundled template +
   `render/assemble-cockpit.mjs`, agent fetches data and bakes it in, credit-memo-style; (c) the repo
   as single source. The plugin's skill/bundle is **stale at `6eda1b6` (Jul 26)** — outstanding item 1.
3. **Migrated to two-seat development**: this repo now lives at `cowork-offering/customer-360`
   (private; pushed as fabiangoetzens). Banksy (`ubuntu@98.87.86.133`) carries the clone at
   `/home/ubuntu/projects/customer-360` with working GitHub push auth, all 39 knowledge docs in
   `/opt/brain/knowledge/projects/customer-360/` (hybrid recall verified 0.99-1.00), and ontology
   nodes `project:customer-360` / `repo:cowork-offering-customer-360` linked `part_of` to
   `product:customer-360` and from `server:customer-360-mcp-salesforce-native`.
4. **Functionality is COMPLETE and live.** Nothing in the outstanding list is missing product
   function: it is packaging (plugin sync), sign-off (founder click-through, Codex review), and
   deliberately-gated items (covenant execute, mod/renewal execute, the renewal-clone re-probe).

## Operating rules for Banksy sessions (non-negotiable)

- Everything under `/opt/brain` and `/home/ubuntu` lands **ubuntu-owned; never sudo git / sudo cp**
  without chown (two incidents on record).
- **Org writes:** the bankinggpt org is a shared Accenture sandbox. Never touch pre-existing org
  build. Additive deploys only, with Created/Changed receipts quoted. Probes on throwaway data with
  residue-zero proof. sf CLI auth lives on the **Archy box**, not Banksy — org work either runs from
  Archy or gets its own auth deliberately.
- **Wire observation is law:** no cockpit payload shape is pinned without a verbatim observed
  request/response envelope (archives in `knowledge/sf-build-v2/wp2/`).
- **Publish pipeline:** only `app/scripts/assemble-artifact.mjs` (full-tag marker match, slot
  re-verified). The claude.ai artifact URLs are republishable only from their owning conversation or
  with the `url:` parameter; expect a 409 conflict guard if another session published newer — never
  force without founder say-so.
- **Quality gates before any publish claim:** typecheck, full vitest, contrast check, clean-room
  `npm ci` build. Report numbers, not prose.
- Fabian style: short answers, no em dashes, decisions surfaced not re-explained.

## The work queue (same as STATUS.md, with entry points)

| # | Item | Entry point |
|---|---|---|
| 1 | Plugin sync to HEAD (bundle + live-data + skill prose says 8 tools, 24 exist; bump 0.4.1 → 0.5.0) | `.claude-plugin/plugin.json`, `skills/credit-360-cockpit/SKILL.md`, `artifact/` |
| 2 | Founder click-through incl. real staged modification on a Hartwell booked loan | Published artifact URL, Hartwell `001bb00001I7FPNAA3` |
| 3 | Codex adversarial review of the campaign delta | Prompt pattern in the Archy brain; wire-payload focus |
| 4 | ⚠️ Re-probe renewal clone field set (`Loan_Collateral_Aggregate` not excluded) BEFORE execute_renewal ever unblocks | `knowledge/PROBE-LEDGER.md` wave method |
| 5 | Covenant execute first live run (fires real approval email — founder-gated) | HANDOFF §4 |
| 6+ | Housekeeping per STATUS.md | — |

## What NOT to do

- Do not write the Dreamforce demo walkthrough (explicit founder hold).
- Do not unblock mod/renewal execute (LV06/Booked wall is by design).
- Do not "fix" latestComplianceId nulls on Hartwell (correct by design; compliance-row creation
  fires an unrecallable approval at a real human).
- Do not delete anything in the Hartwell relationship (permanent deliverable).
