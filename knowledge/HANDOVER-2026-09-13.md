# Credit 360 handover, 2026-09-13 (after 0.9.19)

Read HANDOVER-2026-09-12.md first for the frame; this note only carries what changed after it.

## State
- Released **0.9.19** = commit `3cdde46` on `main`, pushed to `cowork5` (cowork-offering/credit-360-reinvented). 112 files, +18,751 / -1,117.
- Gates at release: tsc clean; vitest 198 files, 4,691 passed, 0 failed, 11 todo; bundle 1.863 MiB (baseline reset to 1,952,974 B, HARD 2.0 MiB, SOFT +50 KB); `grep -ci iris` on the template = 0; vendored memo agents clean; template synced byte-identical.
- **New mandatory gate:** the browser drive of the BUILT page. Script: the session scratchpad `spread-e2e.mjs` (copy it into `design/probes/` next time; it takes `<bundle.html> <csv|xlsx|pdf>`), fixtures `piedmont-fy2025.{csv,xlsx,pdf}`. It caught six defects the unit suite missed (see STATUS.md 0.9.19). Lesson in the brain: unit fixtures for `bundle.boom` must be the real account from `artifact/live-data.json` (only Piedmont carries a raw `spread.file`; Hartwell, Brightwater, Sterling, Kingsley carry display periods only).

## What Fabian must do next
1. Pull the plugin, "open Credit 360" on claudeeshannon (publishes a fresh 0.9.19 cockpit; the old artifact is frozen at 0.9.18), share it once via the page's Share control, and send the new URL so it becomes canonical in `client-360/assets/cockpit.json` (the stored URL is stale, old account).
2. Test: the sixth FAB satellite "Spread financials"; drop a PDF / XLSX / CSV; the provisional badge on the Financials tab; the stress script A/B/C/D against the fixed engines; latency feel (room open ~550 ms to composer, chat streaming).
3. OK or amend the Noland Boom contract draft (scratchpad `noland-boom-contract-DRAFT.md`; content = BOOM-UPLOAD-SPEC.md §2 + §3 + the tool block in `channel/boomUpload.ts`). Not sent.

## Decisions taken by the orchestrator on Fabian's behalf (all logged in the brain, all revertable)
- Correction supersedes the earlier staged entry (stress script A3.k). Overrides the old manifest.ts note.
- Timing constants moved for speed on the "110% zero latency" brief: LOOKUP_MS adaptive (400 floor, 1500 ceiling), COMPOSE_FLOOR only when the desk was asked, arc 0.42 s -> 0.28 s with the lens standing down, chat via the shared pacer.
- DOCTRINE_BUDGET_BYTES 19,000 -> 20,000 -> 20,500 (three moves on 2026-09-12; measured 19,983; `credit-policy` pinned to survive).
- Six-seat FAB arc respread r=124 -> r=150 (46.9 px rhythm kept). Alternative if disliked: a Client Actions row entry.
- Covenant challenge NOT recomputed on a provisional spread until it can carry a provisional qualifier (backlog 20).
- Every period the spread carries lands, each flagged provisional; display-level merge on books without a raw file.

## Open (backlog rows)
19 asset store (needs `assets` capability + a door) · 20 covenant challenge provisional qualifier · 21 cold-open door race (measure in the real host first) · 22 latency hygiene · 25 sibling-year end date · 26 LTM row ordering on the trend after a spread · 27 "on file" marker on tiles the new period cannot fill. Also from 0.9.12: feedback pill in MemoRoom; probe scenario 14 (room-level Boom ladder in drive-lanes); confirm post-booking archival with one real booked modification.

## When Noland's Boom MCP server lands
`channel/boomUpload.ts` top block ("THE MAPPING NOLAND'S SERVER AMENDS"): map the four tool names + arg names, set `BOOM_UPLOAD_LANE = "live"`, flip `SERVERS.boom` to `"Boom"` in `channel/mcp.ts`, drop "IDB Gateway" from required connectors in `plugin.json` (it then serves only the Bedrock fallback rung, which session Claude supersedes), run `design/probes/drive-boom.mjs` and the browser drive, ship.
