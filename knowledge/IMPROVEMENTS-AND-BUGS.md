# Credit 360 — improvements & bugs backlog

Raw intake = the cockpit **Feedback bucket** (`bugs` collection in each cockpit's store, read with
`read_db`). This file is the **curated, triaged** list. Updated as reports come in.

## How Fabian reports (so the transcript is useful)
- Click **Feedback** *once the exchange is underway* (not on the opening screen) — the transcript
  captures the moment you click, so an early click grabs empty `[opening]/[packages]/[brief]`.
- Pick the closest category, one line of "expected vs happened", submit. One issue per report.

## Open — prioritised

| # | Pri | Area | Item | Status |
|---|-----|------|------|--------|
| 1 | **P1 (weekend, pre-Dreamforce)** | Modification | In-flight & post-booking package/exposure lifecycle — lock the original as "Modification in Progress", in-flight editable until approval, booked → old archived + exposure = new set only. Spec: `PACKAGE-LIFECYCLE-SPEC.md`. | Spec'd |
| 2 | P3 | Feedback | Transcript capture reflects click-time; early clicks capture empty. Consider capturing from room mount, or a hint. | Noted |
| 3 | P1 | Modification | **"Weird back-and-forth" / re-ask loop + "connect IDB Gateway" prompt** (bug `bug-1789112493629`). Root cause: the engines' `restate` assist called IDB Gateway DIRECTLY (not session-first); each parser miss waited up to 12s on the flaky bridge and often returned null → the room re-asked. | **FIXED 0.9.16** — restate now session-Claude-first, gateway fallback. Needs a FRESH publish from 0.9.16 to reach a viewer (artifact code is frozen at publish). Re-test: Fabian. |
| 4 | P2 | Modification | Filing "failed" (07:40 + 12:57 runs, Customer 360 connected). **ROOT CAUSE FOUND** in `cm_Action_Staging__c.cm_Tracker_JSON__c`: step `roll_package` → *"nCino refused the credit action: The request contains invalid facilities."* Cause: yesterday's test roll wrote `LLC_BI__LoanRenewal__c` chain rows (parent→clone, `HasActiveRenewalLoan=true`); deleting the clone package + loans left those rows behind, so the parents' `hasRenewal` (FORMULA) / `Number_Of_Renewals` (ROLLUP) still read true/1 → nCino refused to roll them again. **FIXED (org data):** deleted the 12 Hartwell + 4 Meridian orphaned chain rows; both sets now `hasRenewal=false`, `0`. Lesson saved to memory: a clone cleanup must also remove its `LoanRenewal` rows. "Approval closed" on the finale = the cockpit's governance closing after a failed filing (consequence, not cause). | **Fixed — re-test** |
| 6 | P3 | Data hygiene | 19 other loans in the org read `hasRenewal=true` from PRE-test chain rows: mostly 2025 legacy demo accounts (BlueSky*, Bright*, Cy LTD, Summit, Horizon, Ironclad, Global, Doc Prep — outside the confined demo book) plus EverPetal / Flowers For Dreams (Feb 2026) and **Piedmont Precision — LOC $5,000,000 (chain row 2026-06-04, a June-era test)**. NOT touched — legacy rows aren't ours to clean, and the Piedmont one may be an intentional demo state. If a modification on Piedmont's $5M LOC is planned, its chain row must be cleared first or it hits the same "invalid facilities". **Founder decision 2026-09-12: leave the legacy accounts alone; only residue from OUR test-account fleet (Hartwell, Meridian, …) gets cleaned.** | **Decided — leave legacy** |
| 5 | P3 | Modification | Brain-narrated option chips (e.g. "$15.0MM Line of Credit") arrive as free text and can miss the deterministic parser, forcing the restate assist at all. Chips the room offers should carry a canonical, deterministically-parseable `say`. Design improvement, adjacent to P1. | Noted |

## Rule 1 ("knows the full relationship + everything around it") — status and completion

Audit truth (2026-09-12, 0.9.17): NOT yet. Three context builders over one bundle DISAGREE (workroom
envelope carries covenant thresholds/collateral/parties but drops maturities/stages; cockpit chat the
reverse, and drops `notCarried`); the OBLIGOR GROUP (`graph.connections`: guarantors, principals with
ownership %, affiliates) never reached any model; the in-flight version was dropped; the chat could
not state a covenant threshold. On demand (rung 3) the model can pull only `currentBoomRatios` and
`liveInvolvements` (the relationship graph) — never a CONNECTED party's own book.

| # | Pri | Item | Status |
|---|-----|------|--------|
| 7 | P1 | Obligor group into the envelope + `notCarried`; covenant threshold + operator + current value into the chat; in-flight version into the envelope AND the cockpit chat; scope stated when quoting totals; doctrine names each fact. | **FIXED 0.9.18** |
| 8 | P1 | **Connected-party book on demand** — `connectedPartyBook` rung-3 tool: exposure + covenants of a party the anchored graph names, refuses others by name; ladder entry so "the guarantor's own exposure" reaches it. | **FIXED 0.9.18** |
| 9 | P1 (next, after 0.9.18) | **One context builder**: unify the workroom envelope, the cockpit chat context and the relationship-room context into one builder so no two surfaces ever disagree on the same relationship, and every surface gets the full picture (budget-aware). | **FIXED (part 2, 0.9.19, 2026-09-12)** — one builder in `channel/relationshipContext.ts`; `contextUnity.test.ts` + `groundedRecommendation.test.ts`; doctrine budget 19,000 → 20,000 (measured 19,593, `credit-policy` must survive); relationship envelope 9,407 / 10,000 B on Hartwell. |
| 10 | P2 | Recommendation doctrine = **C (founder 2026-09-12)**: recommend only when grounded (on file / doctrine band), never invented. Follow-up pass after 0.9.18 adds the grounded chip/sentence (valuation on-file appraisal, covenant current value, rating bands, renewal tenor). | Decided — follow-up pass |
| 11 | P3 (later) | Salesforce platform + FSC (households, relationship groups, financial accounts) awareness in the envelope/doctrine. | Founder: "not dramatic right now" |
| 12 | Founder confirm | **Design reversal shipped in 0.9.18**: intake falls back to the bundled type mirror (`FILEABLE_COVENANT_TYPES` / `ASSET_KIND_OPTIONS`) when the live catalog read is EMPTY — overturns "org first, mirror never". Rationale: matches elicit's existing `assetTypeUniverse`; required for a connector-less room; the write still verifies at the org. Tests rewritten with dated comments (intakeFlows.test.ts:889). | **Keep (founder, 2026-09-12)** |
| 13 | P2 (part 2) | Permanent test cover for the relationship fixer's items 8 (rating asks), 9 (service-request peek), 11 (polish) — verified with a deleted harness only. | **FIXED (part 2, 0.9.19, 2026-09-12)** — one builder in `channel/relationshipContext.ts`; `contextUnity.test.ts` + `groundedRecommendation.test.ts`; doctrine budget 19,000 → 20,000 (measured 19,593, `credit-policy` must survive); relationship envelope 9,407 / 10,000 B on Hartwell. |
| 14 | P2 (part 2) | Cockpit chat: a standing `notCarried` list (today it names gaps only via the cut notice); ids on the `reads.group` rows so the model can address `connectedPartyBook` by id, not only by name (~80 B on Hartwell). | **FIXED (part 2, 0.9.19, 2026-09-12)** — one builder in `channel/relationshipContext.ts`; `contextUnity.test.ts` + `groundedRecommendation.test.ts`; doctrine budget 19,000 → 20,000 (measured 19,593, `credit-policy` must survive); relationship envelope 9,407 / 10,000 B on Hartwell. |
| 15 | P2 (part 2) | Grounded-recommendation WORDING per doctrine C where a figure is on file or a doctrine band exists (valuation appraisal, covenant current value, renewal tenor); the on-file CHIPS already ship in 0.9.18 — this is the "use it" sentence. | **FIXED (part 2, 0.9.19, 2026-09-12)** — one builder in `channel/relationshipContext.ts`; `contextUnity.test.ts` + `groundedRecommendation.test.ts`; doctrine budget 19,000 → 20,000 (measured 19,593, `credit-policy` must survive); relationship envelope 9,407 / 10,000 B on Hartwell. |
| 16 | Founder decision | **Bundle-size gate**: moved twice on 2026-09-12 (1.75 → 1.76 → 1.77 MiB) for legitimate feature growth. Proposal: replace 0.01-MiB nudges with a real load-derived HARD cap (e.g. 2.0 MiB, "still a page over a hotel connection") plus a SOFT "justify" tier at +50 KB per release, so the gate catches bloat without taxing every feature. **Founder accepted 2026-09-12; applied in `app/scripts/rename-bundle.mjs`: HARD 2.0 MiB (fails), SOFT +50 KB over the shipped baseline `BASELINE_BYTES` (prints JUSTIFY, passes; bump the baseline in the release commit).** | **Decided, applied (ships with 0.9.19)** |
| 17 | P1 (GO, founder 2026-09-12) | **"Spread financials"** upload room: FAB action, file drop, pre-read analysis (what/whose/when/quality) with relationship context, governed plan, Boom adapter (stub until Noland's connector), post-read vs on-file + covenants, spreads visible in every workroom + memo. Spec: `BOOM-UPLOAD-SPEC.md` §3; contract: `app/src/spread/types.ts` (Boom-API-shaped). **No Boom connector exists yet: everything runs on the stub.** | **SHIPPED 0.9.19 on the stub** (room, pre-read, post-read, adapter, publish, activity trail, honesty suite, browser gate) |
| 18 | P1 (ships with 17, founder confirmed 2026-09-12) | **Spread lands in the Financials tab, memo graph and covenant challenge.** `FinancialsTab` renders `bundle.boom` (normalised `boom_get_spread` + `boom_get_ratios`). With the real Boom server a re-read refreshes it; with the STUB the room must publish the returned `financialStatements` into the same `bundle.boom` slot through `boom-normalise.mjs`, flagged provisional, so the new period shows on the tab with a "provisional, Boom verification pending" badge and the memo graph + covenant recompute pick it up. Test: Financials tab renders the new period from a stub spread. | **FIXED 0.9.19** (display-level merge on the real Hartwell bundle; badge names the newest provisional period) |
| 19 | P2 (after 0.9.19) | **Spread files into the artifact asset store** (spec §2 audit trail). Blocked on two facts verified 2026-09-12: the page declares only `mcp`, `sample`, `db` (`client-360/assets/capabilities.json`) and `channel/` has no asset door. Needs: declare the `assets` capability; add an asset door acquired like the other three (`claude.use("assets")`, once, bounded, absent-is-not-an-error); then the fire-and-forget write at the marked TODO in `workroom/spreadEngine.ts` (bytes + sha256 already in hand). Failure never blocks the plan. | Open |
| 20 | P2, honesty gate first | **Covenant challenge does not move on a published spread.** `bundle.covenantChallenge` is baked by the assembler (`client-360/render/validate-c360.mjs buildChallenge`) and nothing in the app recomputes it, so after a spread lands the Financials tab, memo graph and the room move but the Covenants tab still challenges against the assembler's spread. Wiring is three small steps (export `buildChallenge`, add `validate-c360.d.mts`, call it in `state/spreadPublish.ts applySpreadEvent` in a try/catch). **Precondition:** the challenge entry needs a provisional qualifier (assembler-side) so a stub-provisional period never prints an unqualified "Boom-implied … corroborates" SR 11-7 line. Do not switch on before that. | Open (decision: honesty first) |
| 21 | P2, needs real-host measurement | **Cold open inside the artifact host blocks the mount on `Promise.all([acquireMcp, acquireSample, acquireDb])`, each with a 4 s internal timeout** (`main.tsx`); worst case a blank page for 4 s (sampleDoor records ~10 s once). Reason is real: `appState.tsx` reads `useLivePortfolio(mcpAvailable())` non-reactively. Fix: race the doors against a ~600 ms floor and add a `doorsReady` tick (tiny `useSyncExternalStore`, same shape as `roomSession.ts`) so the synchronous gates re-read once. Measure `claude.use()` latency in a real Cowork session before choosing how aggressive. L1 audit 2026-09-12. | Open |
| 22 | P3 | Latency hygiene from the L1 audit (2026-09-12): `.flyname` animates left/top/font-size (C3, marginal); 20 `transition: all` sites across stylesheets (C4); composer draft at the top of each room re-renders the whole room per keystroke (F2, measured fine on an empty manifest, unmeasured on a full one); landing long tasks 51-119 ms for a 2 MB single-file page (F3). Profile before fixing. | Noted |
| 23 | Decided (orchestrator on the founder's stress script, 2026-09-12) | **A correction supersedes the earlier staged entry.** "7%" then "actually 8%" now replaces the entry at the same wire address in place (modify and renew), landing line "That replaces the earlier 7%.", and the plan sends the latest figure only. Overrides the older `manifest.ts` note "amending is not a third move: remove and say it again", which the stress script (A3.k, "stacks both" flagged) made obsolete. Covenants, pledges, fees and parties still stack (different wire addresses). | **FIXED 0.9.19** |
| 24 | Fixed 0.9.19 | Chat and workroom fallback audit (A1 + A2 + F1, 2026-09-12): 16 + 21 findings, all P1 fixed with repro suites `chatFallback.audit.test.ts` (25), `workroomFallbacks.audit.test.ts` (31), `fixerPass.audit.test.tsx` (25). Headlines: chat showed "Chat unavailable" with the session door open; desk wait was unbounded (now 75 s, bounded and honest); relative moves ("add 50bps", "increase by $5M") were staged as absolute figures and a minus sign was dropped; a blank or gibberish line destroyed the pending question; new-facility typed amounts were lost silently; "Which collateral" went out as a chooser with zero options; connector wording ("Add IDB Gateway…") reached the KPI band and the actions panel; term / first-payment asks led with no current figure. | **FIXED 0.9.19** |
| 25 | P2 | Spread pre-read: a column whose header prints only a year ("2025, 2024" under "Fiscal year ended December 31, 2025 and 2024") gets an `endDate` only for the year the sentence names; the other period carries `endDate: null` and `boom-normalise` drops it, so of a two-column drop only the newest lands. Fix in `spread/periods.ts`: derive the sibling year's end date from the same month/day when the sentence names one fiscal year end. Never invent a year end where none is printed. | Open |
| 26 | P3 | Financials tab after a published spread: the book's LTM display row sits last, so the trend dips after the new FY2025 point and the headline still reads the LTM figure; `boom.note` still names the on-file workbook. Decide ordering (by end date where known, LTM anchored to the newest FY) and a note that names the dropped file. | **FIXED 0.9.20** (newest spread is the latest point; LTM sits before it). `boom.note` file name still open. |
| 27 | P3 | Spread panel tiles keep the on-file EBITDA / leverage where the new period carries no D&A (honest, but the tile should say "on file" beside the figure). | Open |
| 28 | P1 (0.9.20) | **Spreading room UX + narrative (founder test of 0.9.19 in the browser, 2026-09-13):** the send step sat in the middle with the Financials panel already showing provisional tiles below it; the file card said the same fact twice; "Send to Boom" was a solid button outside the glass register; the room panel trend showed FY2025 twice (no label merge); the post-read listed facts but connected nothing. Fix: five-step guided flow (Drop, Read, Confirm, Boom, Financials), drop zone collapses after the first file, curated card, the plan with a glass action as the LAST element, ladder morphs in place, panel reveals only when the spread lands, label-merged trend, and "connect the dots" post-read: every covenant on the book re-tested where derivable, three-period trend, balance-sheet and margin moves, rule-generated committee questions, one model paragraph rephrasing those facts only (guard unchanged). | **FIXED 0.9.20** (S8 flow + narrative, S9 finale sheet + doors + trend order) |
| 29 | P2 (roadmap) | **Document drop router.** The Spreading room takes financial statements only. An appraisal PDF belongs to collateral valuation, a compliance certificate to covenant review, a tax return is a statement of its own kind. One drop surface that classifies the document (pre-read already tells type) and routes it to the right room, so the banker never has to know which door a file goes through. Founder question 2026-09-13 ("is this only for Boom or also covenants, collateral?"). | Open |
| 30 | P2 | **Sync forces every lane** (founder report 2026-09-13: a modification showed only after Sync and a reload). The Sync gesture bypasses the slow-tier skip window; the open path keeps it. | **FIXED 0.9.20** |
| 31 | P2 | **Connector grants front-loaded at open** (founder report 2026-09-13: prompts came one by one per page). After first paint, one cheap read per configured connector so every permission prompt comes at the start, once. | **Built 0.9.20, OFF by default from 0.9.22** (`GRANT_WARMUP_ENABLED=false`): prime suspect for the real-host "Composing for 3 minutes" report of 2026-09-13; a real-host run must prove the host does not queue the session door behind an open consent dialog before it is switched on. |
| 32 | Fixed 0.9.20 + follow-ups | **Memo gap audit** (founder 2026-09-13: "a lot of not-in-source-system"). `memo/gapAudit.test.ts` classifies every marker on the real Hartwell and Piedmont bundles. Class A filled in 0.9.20 (borrower profile, guaranty type, display-book Key Metrics + charts labelled as displayed, supporting documents, before-key semantics; narrative FIGURES enriched). **Class B, needs a read change (Apex, out of scope here):** `Account.NaicsDesc`, `Account.Description`, `Product_Package__c.Name` on Snapshot; rating history (new read); `Legal_Entities__c.Id` on RelationshipGraph (guarantor deep link); `Term_Months__c`, `Amortized_Term_Months__c`, `First_Payment_Date__c` on Exposure; AFS servicing coordinates on the bake. **Class C stays marked by doctrine.** Vendor-side notes for Noland's renderer: zero-debt leverage arithmetic, FCF from two lines a display book lacks, one guarantor rendered where the package has three, `_source` label never printed. | Fixed (A) / Open (B) |
| 33 | Fixed 0.9.21 | **Memo Key Metrics table** (founder review 2026-09-13): repeated "as of" ratios across columns with "(unchanged)", three-column cap, covenant test in a fiscal column, three gap vocabularies, and Debt ÷ EBITDA 2.17x vs Boom's 3.85x (dossier picked the current-portion line as long-term debt). Fixed post-render via the override seam; `keyMetrics.test.ts`. Vendor-side for Noland's renderer: `render-memo.mjs` 387 (slice(-3)), 411-412 (hardcoded pro forma), 413-416 (vocabularies, DSC in a fiscal column). | **FIXED 0.9.21** |
| 34 | **P1 (live, 0.9.22)** | **Modification room locks after a rate hold** (feedback bucket `bug-1789294443785-7nd049`, Hartwell, 0.9.20, 2026-09-13 10:14 UTC). After "Hold 6.58%" every later input ("what borrowers are on this loan already?", "yes increase to 7.25%", "show me the pledges") got "One decision at a time. The open card above, or the review chip under it, carries the next move." with no visible open card. `Workroom.tsx` ~1949 `openGates` counts a live item that stayed live after the hold; questions are blocked by the gate instead of answered; a change of mind after a hold is refused. Also reported: the cockpit chat did not come back with an answer. | FIXED 0.9.22 |
| 35 | P1 (0.9.22) | **Thread pile-up**: spent chip rows and "Filed: [settled]" rows stack above the live question in every room; founder wants only the current action shown, earlier turns as one-line recaps (expand on click); memo room opening chips too wide. | FIXED 0.9.22 |
| 36 | Backburner (founder, 2026-09-13) | **Voice in the cockpit** (Dreamforce "wow" idea, parked). Hard limits: the artifact sandbox allows no fetch / WebSocket / XHR, so no duplex realtime voice API from the page; MCP is request-response. Two spikes decide feasibility (30 min each, real host): (1) microphone permission inside the artifact iframe, (2) speech recognition in Claude desktop's Electron. If both pass: push-to-talk pill (the FAB morphs into a glowing waveform pill in our glass register, keyboard chord), browser STT 300-800 ms, text down the existing composer rails (parsers first, session Claude second: rooms, chips, staging already work as text), replies via the OS voice (instant) or a premium Hume voice through an MCP tool returning audio (1-2 s, no streaming). 3-4 days after the spikes. Not interruptible, not always-listening. If a spike fails: only Claude desktop's own dictation into the chat. | Parked |

## Stress-test script — run these, report each detail

### A. MODIFICATION (the primary path)
1. **Single-package borrower** — start a modification; does it bind the one package silently and *say
   it did*, or wrongly ask?
2. **Multi-package borrower** (Hartwell, Meridian) — does it ask which package? Are booked packages with
   an **in-flight version** shown as **"Modification in Progress"** and un-selectable? (this is P1)
3. **Forced rate** — at the rate ask: (a) type a new rate `7.25%`, (b) click the **Hold** chip, (c)
   *type* "hold" / "keep it" / "no change". All three should behave (set vs keep); none should loop.
4. **Amount** — increase and decrease a facility; use the **"Keep <current>"** chip; also *type* "keep".
5. **Term / maturity** — change amortised term and first payment date; keep-chips on each.
6. **Guidance quality** — every question should lead with the **current figure**, offer **options**, and
   **recommend** one. Flag any question that just asks blankly.
7. **Garbage input** — type gibberish, a negative, an absurd number, letters into the rate. It must
   re-ask gracefully, never loop or accept nonsense.
8. **The finale** — glass buttons; "Draft the credit memo" opens the memo (source reads **AFS**, never
   IRIS); "Back to <account>"; the **"Open the new package in nCino"** link resolves.
9. **Plan accuracy** — does the staged plan match EXACTLY what you asked (rate, amount, term, maturity)?
10. **Cross-flow lock (P1 repro)** — file/stage a modification on a package, then start ANOTHER
    modification on the SAME package → it must refuse ("Modification in Progress" / "book or discard in
    Salesforce first"), not fork a second version.
11. **Exposure after booking** — if you can get a modification to booked: does exposure show the **new**
    figure only, or old+new (double-count)? (this is the deep P1 case — gold if you can reproduce it)
12. **Feedback pill** present in the modify room header; transcript captures the exchange.

### B. RENEWAL
1. Pick a facility **near maturity** (Sterling has one) and start a renewal.
2. Change maturity, rate, commitment — same **current-figure + options + recommend** guidance + keep-chips.
3. Does it lead with the **current** commitment / rate / maturity before proposing?
4. Garbage-input + typed-hold behave.
5. Finale + new-package link.

### C. NEW FACILITY
1. Start a new facility — does it correctly **create a new package** (or add to an existing one), and is
   that choice clear?
2. Product type, amount, purpose, rate, term — each guided (options, recommend).
3. **US vs non-US / collateral** path — this is a known soft spot; watch for a loop or a vague ask
   ("please provide collateral information") instead of a specific one.
4. Finale + the new package resolves in nCino.

### D. COVENANT REVIEW / COLLATERAL VALUATION (relationship room)
1. **Covenant review** — the read card should be aligned (not stretched); package-scoped; the failing
   covenant pre-seeded with its current test vs threshold.
2. **Collateral valuation** — the ask should be **specific** ("needs a current valuation record for asset
   X; appraisal on file: $Y, date Z"), not vague; watch the US/non-US loop.
3. Feedback pill present (added 0.9.14).

### Cross-cutting — watch on EVERY flow
- **Looping / repeating** — any question asked twice, or a "hold" that re-asks.
- **Figures** — every number should trace to the book; flag any that looks invented or double-counted.
- **Voice** — sober banker tone, no marketing fluff, no exclamation points.
- **KPI/EWS popover** — clicking a tile opens the right room **pre-seeded** to the right account/ask.

## Done (recent)
- 0.9.9 stale-template fix · 0.9.10 KPI/EWS popover · 0.9.11 glass finale · 0.9.12 Feedback pill visible
  · 0.9.13 Feedback bucket · 0.9.14 Feedback in relationship room · 0.9.15 IRIS purged (→ AFS).
| 37 | P2 (0.9.23) | **Stress drive findings in the facility room** (design/probes/workroom-e2e.mjs, stressRate + relativeAndSign on the built 0.9.22 page): a singular "the line of credit" staged BOTH Hartwell LOCs on the stub book; "add 50bps on the line of credit" read as a term question; an unreadable answer repeats the term question verbatim; "keep it" / "no change" typed do not land as the keep chip; a member question without chips; a date typed during a member question re-asks the term. | FIXED 0.9.22 (all six on the built page); left: a bare date typed at an idle room cannot tell maturity from first payment date and does not ask |
| 38 | P1 (0.9.23) | **Create room**: opened from inside an in-flight package it offers only that package and no way to a new package with a loan; the joinable rule ignores the in-flight lock, so a booked package with a version in flight is offered as a home for a new facility. Founder 2026-09-13. Spec: knowledge/SPEC-0.9.23-VERSION-LIFECYCLE.md 2c. | Open |
| 39 | P1 (0.9.23) | **Shape the version**: no route amends an editable in-flight version or a cockpit-created package (facility figures, add covenant, add collateral); the refusal sentence promises "change the figures in this version" and nothing does it; covenant/collateral pickers block every unbooked package. Founder 2026-09-13 ("I thought P1 built this"; P1 built the state, not the routes). Spec 2a. | Open |
| 40 | P1 (0.9.23) | **Undo the version**: no discard/rollback of an in-flight modification exists in the cockpit or in our Apex; the only proven path is the manual sf CLI chain of 2026-09-11 (clone loans + version package + LLC_BI__LoanRenewal__c rows + staging rows). Needs DiscardVersion stage/execute with inventory, ordered delete, re-query proof, "Withdrawn" on the trail. Spec 2b. | Open |
| 41 | P3 | **One ratio, two figures** after a stub spread: coverage 3.09x in the room panel vs 2.95x in the Financials tab Key ratios (spread-e2e pdf, 0.9.22 gate). Root cause: the tab and the memo printed the on-file LTM ratio while the room struck the drop's own period; Boom's own definition is operating profit over interest expense (proved against Piedmont's snapshot: 2,838,000/1,076,000 = 2.6375 = Boom's raw.interestCoverage), one definition now in `spread/coverage.ts`, restruck on publish only when the spread carries the newest period. | FIXED 0.9.22 |
| 42 | P3 | **Key ratios card mixes periods after a spread**: FY2025 revenue and coverage (restruck from the drop) sit beside LTM EBITDA, margin and leverage (not derivable from statements), with no period label per figure. Honest per field, unlabeled as a card. | Open |
