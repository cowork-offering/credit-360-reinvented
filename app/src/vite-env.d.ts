/// <reference types="vite/client" />

/** The commit this bundle was built from, injected by vite's `define`. See
 *  vite.config.ts: it is the only thing that can tell a founder looking at a
 *  pinned artifact which build is in front of him. */
declare const __C360_BUILD__: string;
